# Frontend environment variables (Vite)
# Copy to .env.local and fill in values

# Backend API URL — leave blank when using Vite proxy (local dev)
# Set to your Railway URL for production
VITE_API_URL=
